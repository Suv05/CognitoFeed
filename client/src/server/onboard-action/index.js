"use server";

export const completeOnboarding = async (data) => {
  console.log("Server action received data:", data);

  // Authentication and onboarding functionality removed
  return {
    success: true,
    message: "Onboarding data received",
  };
};
